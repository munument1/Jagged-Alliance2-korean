# JA2 r7609 Korean Patch v0.1.6-alpha

이번 릴리스는 기존 번역 데이터와 설치기를 유지하면서, 한국어 `ja2.exe`의 런타임 문제 두 가지를 수정한 알파 핫픽스입니다.

## 핵심 변경

### I.M.P. 한글 이름 입력 수정

- I.M.P. 캐릭터 생성 화면에서 이름과 별명이 영문 ASCII 입력으로만 제한되던 문제를 수정했습니다.
- 기존 영문/숫자/기호 입력은 그대로 유지하면서 한글을 포함한 Unicode 입력을 허용합니다.
- `PGmale*`, `PGLady*` 같은 문자열은 I.M.P. 내부 슬롯 식별자이므로 번역하지 않습니다.

### Fleuropa 꽃집 배송지 선택 수정

- 꽃집 주문서에서 배송지 드롭다운의 실제 클릭 영역이 WinFont 표시 영역보다 좁아 도시 선택이 불안정할 수 있던 문제를 수정했습니다.
- 각 배송지 행의 클릭 범위를 다음 행과 겹치지 않는 범위까지 확장했습니다.
- 배송지 데이터, 배송비, Bobby Ray 배송 시스템은 변경하지 않았습니다.
- Meduna는 기존 r7609 데이터에 정상적으로 존재하며, 배송지 인덱스 7 선택 시 Deidranna 꽃다발 이스터에그 로직이 실행되는 것도 확인했습니다.

## 실행 파일 검증

최종 `Patch/ja2.exe`:

- 크기: `8,407,552 bytes`
- SHA-256: `a3480fd92a6c5e4e184b367cf29705d52b8b129a616c6a6a80affd062ee77582`
- 기존 한국어 EXE 대비 실제 변경 바이트: `27 bytes`

기존 한국어 내장 UI 문자열과 WinFont UI 보정은 그대로 유지됩니다.

## 소스/재현성

이번 수정과 함께 r7609 기준 소스 복원 정보를 저장소에 보존했습니다.

- 기준 소스: `1dot13/source@af1f5c5e173382b5070494e3414bcf5145fd9a6b`
- SVN marker: `Build@7606`
- 빌드 기준: Visual Studio 2013 / v120 / Win32 Release
- 소스 패치 도구: `tools/apply_r7609_runtime_hotfixes.py`
- 현재 한국어 EXE 패치/검증 도구: `tools/patch_current_korean_exe.py`
- 상세 기록: `R7609_SOURCE_RECOVERY.md`

## 포함 내용

이 패키지는 누적 배포판입니다. 기존 한국어 번역 리소스, BASE NPC 대사, MercEdt, EnemyTaunts, AIM/IMP/퀘스트/RIS 번역, Galmuri 글꼴 설정과 설치기 수정 사항을 그대로 포함합니다.

## 설치

대상 버전은 **Jagged Alliance 2 v1.13 r7609 영어판**입니다.

1. `JA2_r7609_Korean_Patch_v0.1.6-alpha.zip`을 압축 해제합니다.
2. `install.bat`, `install.ps1`, `Patch` 폴더를 JA2 r7609 설치 폴더로 복사합니다.
3. `ja2.exe`와 `Ja2.ini`가 있는 게임 폴더에서 `install.bat`을 실행합니다.
4. 기존 실행 파일과 INI는 설치기가 자동으로 백업합니다.

## 알려진 사항

- 이번 두 수정은 소스 및 바이너리 수준에서 정적 검증했습니다.
- 세이브 부재로 Meduna 꽃다발 이벤트 전체 진행과 저장/불러오기까지의 I.M.P. 한글 이름 유지 여부는 릴리즈 직전 실게임 전수 검증을 하지 못했습니다. 문제가 확인되면 후속 알파에서 즉시 수정합니다.
- 다른 JA2 1.13 리비전이나 대규모 모드 조합에서는 호환성을 보장하지 않습니다.
